spring:
  datasource:
    url: jdbc:mysql://localhost:3306/blog_dev
    username: root
    password: admin123
  flyway:
    enabled: true
    baseline-on-migrate: true  # Add this to handle existing data
    locations: classpath:db/migration


  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true